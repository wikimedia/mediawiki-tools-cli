include:
  - local: .gitlab-ci.commit.yml
    rules:
      - if: $CI_PIPELINE_SOURCE == 'merge_request_event' || $CI_PIPELINE_SOURCE == 'push'
  - local: .gitlab-ci.daily.yml
    rules:
      - if: $GITLAB_SCHEDULE == 'daily'
