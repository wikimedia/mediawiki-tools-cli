# Grafana service

Using this will automatically configure a Grafana service for visualizing metrics from Prometheus.

When running, this will integrate with Prometheus to provide metrics dashboards.

## Requirements

The Prometheus service must be running for Grafana to collect metrics.
